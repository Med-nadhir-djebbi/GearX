{% extends 'base.html.twig' %}

{% block title %}Payment Canceled{% endblock %}

{% block body %}
    <h1>Payment Canceled</h1>
    <p>Your payment was canceled. You can retry the payment or check your order status.</p>
    <a href="{{ path('account_orders') }}" class="btn btn-primary">Go to My Orders</a>
    <a href="{{ path('cart') }}" class="btn btn-secondary">Return to Cart</a>
{% endblock %}