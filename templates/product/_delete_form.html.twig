<form method="post" action="{{ path('admin_products_delete', {'id': product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="w-100">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="gaming-button btn-danger w-100">DELETE</button>
</form>
